var donuts = ["jelly donut", "chocolate donut", "glazed donut"];
var improvedDonuts = donuts.map(function(donuts) {
  donuts += " hole ";
  donuts += donuts.toUpperCase();
  return donuts;


})
console.log(improvedDonuts);